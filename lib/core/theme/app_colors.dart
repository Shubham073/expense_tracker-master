import 'package:flutter/material.dart';

class AppColors {
  static const primary = Colors.blue;
  static const income = Colors.green;
  static const expense = Colors.redAccent;

  static const bgLight = Color(0xFFF5F5F5);
  static const bgDark = Color(0xFF1C1C1E);

  static const chartColors = [
    Colors.blue,
    Colors.red,
    Colors.green,
    Colors.orange,
    Colors.purple,
    Colors.teal,
  ];
}
